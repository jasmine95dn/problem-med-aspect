Models:

- general BERT: bert-large-uncased-whole-word-masking -> transformers 
- general Flair: news-forward/backward-fast, wordembedding(glove) -> FLAIR
- clinical BERT: 'emilyalsentzer/Bio_ClinicalBERT' -> transformers
- HunFlair: we('pubmed'), pubmed-forward/backward -> FLAIR
- clinicalBERT+flair(news-forward/backward): concatenate
- HunFlair+BERT: FLAIR, StackedEmbeddings
- BERT+FLAIR: FLAIR, StackedEmbeddings
- clinicalBERT+HunFlair: concatenate
